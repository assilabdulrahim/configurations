################################################################################
# This .gitignore file was automatically created by Microsoft(R) Visual Studio.
################################################################################

/.vs/*
# Claude Code worktrees are working copies of this repo, never content of it.
/.claude/worktrees/
/opencode/.claude/worktrees/

# Fetched catalogs used by verify-config / smoke-agents. Refreshed on demand,
# never committed - they are a cache of models.dev and the ollama box, not config.
opencode/models.json
opencode/tags.json

# Trace store, if ever pointed at the repo instead of ~/.config/opencode
opencode/traces/

# Environment files and credentials
.env
*.env.local
*.local.env

